{
  "name": "sandbox-monorepo",
  "private": true,
  "workspaces": [
    "sandbox",
    "viberanium"
  ],
  "scripts": {
    "dev": "npm run dev -w sandbox",
    "build": "npm run build -w sandbox",
    "preview": "npm run preview -w sandbox",
    "typecheck": "npm run typecheck -w viberanium && npm run typecheck -w sandbox"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.1.1"
  }
}
